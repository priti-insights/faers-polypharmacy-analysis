# Data

The raw FAERS datasets and generated intermediate files are not included in this repository due to their large size.

The complete preprocessing pipeline is provided through the SQL and Python scripts.

Users can recreate the datasets by downloading the FAERS ASCII files from the FDA website and running the import scripts.
